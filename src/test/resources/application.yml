spring:
  datasource:
    url: jdbc:h2:mem:class-registration-test;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
  sql:
    init:
      mode: never
